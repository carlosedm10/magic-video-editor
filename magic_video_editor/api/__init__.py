"""HTTP API routers, split by concern: projects (project/clip/sentence/order),
pipeline (stage running + run-all), and per-feature stubs (settings, audio,
filters, edl) that will grow as their owning feature lands."""
